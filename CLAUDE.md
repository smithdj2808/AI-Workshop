# CLAUDE.md

## Stack
Next.js (App Router), TypeScript, plain CSS, Supabase, deployed on Vercel.

## Commands
- `npm run dev` — local dev server
- `npm run build` — production build
(Confirm these match package.json before relying on them — not yet verified against this repo.)

## Never
- Add a dependency without asking first.
- Edit `.env` or any environment variable.
- Change auth configuration without saying what is changing and why.
- Create new top-level folders.

## Conventions
- Plain CSS only — no CSS framework or CSS-in-JS library without asking first.
- Keep components and pages consistent with the existing App Router structure already in the repo.

## Current focus
See roadmap.md. Work only on the slice marked ACTIVE.
